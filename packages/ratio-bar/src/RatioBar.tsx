import * as React from 'react'

import { Wrapper } from './styles'

interface Props {}

export const RatioBar: React.FunctionComponent<Props> = (props: Props) => {
  return <Wrapper>Ratio Bar Component</Wrapper>
}

export default RatioBar
